# 카드뉴스 키워드 정책 (필독)

> 이 파일은 카드뉴스를 생성하는 모든 에이전트가 반드시 따라야 하는 키워드 규칙이다.

## 규칙

1. **랭킹 키워드(랭킹픽, TOP 50, 베스트 랭킹 등)는 사용자가 명시적으로 "사용해달라"고 말하지 않는 한 절대 사용하지 않는다.**
   - 에이전트가 스스로 판단해서 랭킹 기반 주제/키워드를 고르는 것 금지.
2. **카드뉴스에는 항상 시즌성 키워드를 담는다.**
   - 계절·날씨·시기 이슈에 맞는 키워드 (예: 여름 슈즈, 우양산, 장마 아이템, 가을 자켓 등).
3. 폴더명 형식은 기존과 동일: `카드뉴스\YYYYMMDD 키워드` (예: `20260724 여름 슈즈`).

## 카드 구성 규칙 (필수)

1. **마지막 카드(CTA)의 이미지는 항상 무한도전 짤**을 사용한다.
   - 짤 위치: `fashion-cardnews\CARD\zzal\` — 이 폴더에서 **수정일 기준 최신 파일**을 쓴다.
   - 구현: `renderer.build_html` 이 `copy["cta"]["image_path"]` 를 지원한다 — CTA copy 에
     최신 짤 경로를 넣어 렌더하면 된다 (없으면 기존처럼 첫 상품 이미지 폴백).
2. **헤드라인 하이라이트(<em>)는 의미 단위로** 끊는다. 조사·어미만 홀로 강조하지 않는다.
   - 예 (2026-07-24 실제 피드백): "편한데 스타일도 <em>사는</em>" (어색) →
     "편한데<br><em>스타일도 사는</em>" (자연스러움).
3. **사용자 코멘트(note)는 참고용이다 — 그대로 싣지 않는다** (2026-07-27 사용자 피드백).
   - 코멘트의 **의도·핵심만** 가져와서, 템플릿 감성(짧고 절제된 에디토리얼 톤)으로 다듬어 쓴다.
   - 예: "더운 여름 출근길에도 단정해야해" (구어체 원문) → "더운 출근길에도<br><em>단정하게</em>".
4. **소재·스펙 등 상품 정보는 반드시 상품 상세 페이지를 확인하고 그 내용을 베이스로 쓴다**
   (2026-07-27 실제 발생 — 상세 페이지를 안 보고 상품명에서 추정해 썼다가 지적받음.
   실제 소재는 나일론 100%·메쉬 100%로 상세 페이지에 있었음).
   - 상품 데이터의 `product_url`(무신사/29CM 상세 페이지)을 **WebFetch 로 직접 열어**
     소재/혼용률/핏/디테일 정보를 확인한 뒤 작성한다. 상품명에서 추정하는 것 금지.
   - **29CM 에서 소재표가 안 잡히면** (29CM 상세는 JS 렌더라 WebFetch 로 안 잡힐 수 있음):
     **무신사에서 같은 상품을 검색해 찾고, 무신사 상세 페이지의 소재 정보를 베이스로 쓴다**
     (브랜드명 + 상품명/품번으로 검색. 나이키 등 품번(예: IF0564-248)이 있으면 품번 검색이 가장 정확).
   - 무신사에서도 같은 상품을 못 찾으면 **사용자에게 상세 페이지 내용을 물어보고**, 답을 받기
     전에는 소재 언급을 생략한 채 후기·가격 등 확실한 정보로만 쓴다.
5. **캡션 무드는 매 게시물마다 다르게 쓴다** (2026-07-27 사용자 피드백 — 직전 게시물과 같은
   문형("~부터 ~까지 꼼꼼히 따져 ~ 모았습니다")을 반복했다가 "AI 티가 난다"는 지적을 받음).
   - 캡션 작성 전 **직전 게시물 2~3개의 caption.txt 를 먼저 읽고**, 같은 문형·구조·시작 패턴을 피한다.
   - 무드를 로테이션한다 (예): 정보·큐레이션형 / 공감·일기형("결국 숏팬츠만 돌려 입고 있습니다") /
     질문 후킹형(첫 줄부터 질문) / 날씨·상황 스케치형. 같은 무드를 연속 2회 쓰지 않는다.
6. **캡션 문장은 짧게 끊어 나열하지 말고, 근거를 한 문장에 녹여 쓴다** (2026-07-24 사용자 피드백).
   - 예: "~골랐습니다 / 후기와 평점을 함께 살펴봤습니다" (뚝뚝 끊김) →
     "젤리 샌들부터 클로그까지 후기 수와 평점까지 꼼꼼히 따져 무더위에도 시원하게 신기 좋은 신발들을 모았습니다" (한 문장으로 깔끔).
4. **캡션에는 적절한 이모지를 넣는다** (2026-07-24 사용자 지시 — 기존 "0~1개" 제한보다 우선).
   - 주제와 어울리는 이모지를 문장 끝에 자연스럽게 배치한다 (예: 여름 슈즈 → 🩴 ☀️, 평점 → ⭐, 댓글 유도 → 💬).
   - 브랜드 계정 목록 헤더에는 📌 를 붙인다: `📌 브랜드 계정`.
   - 과하지 않게 본문 전체 3~5개 수준. 해시태그는 여전히 금지.
4. **캡션(피드 멘트) 하단에 브랜드 인스타 계정 목록**을 넣는다.
   - 카드뉴스 **상품 순서대로**, 형식은 한 줄에 `브랜드명 @핸들` (브랜드명과 핸들 사이 띄어쓰기):
     ```
     브랜드 계정
     락피쉬웨더웨어 @rockfish_official
     ...
     ```
   - **가짜 계정 주의 — 핸들은 반드시 3회 독립 검증** 후 사용한다 (아래 검증 규칙 4번 참고).

## 실험·측정 규칙 (필수 — "감으로 판단" 금지)

사용자는 게시물 성과를 감으로만 판단하는 습관을 고치고 싶어 한다 (2026-07-24).
따라서 **게시물 1개 = 실험 1개**로 취급하며, 에이전트가 아래를 강제한다:

1. **게시 전**: 결과 폴더에 `result.md` 를 생성한다 (양식: [RESULT-TEMPLATE.md](RESULT-TEMPLATE.md)).
   - 가설, **바꾼 변수 1개**, 대조군(직전 실험), **숫자로 된 판정 기준**을 게시 전에 확정해서 적는다.
   - 변수는 반드시 1개만 바꾼다. 게시 시간대 등 나머지 조건은 대조군과 동일하게 유지.
2. **게시 48시간 후**: **인사이트 API 로 직접 조회한다** (2026-07-27 권한 확인 —
   `GET graph.instagram.com/v23.0/{media_id}/insights?metric=reach,saved,shares,likes,comments,total_interactions,profile_visits,follows,views`,
   미디어 ID 는 `GET /me/media` 로 찾음. 사용자에게 숫자를 요청할 필요 없음. API 실패 시에만 사용자에게 요청).
   조회한 값으로 **에이전트가 저장률·공유율·참여율·전환율을 계산**하고
   판정(채택/기각/판단불가)과 "배움 한 줄"을 result.md 에 기록한다.
3. **다음 게시물 설계 시**: 직전 result.md 들을 먼저 읽고, 거기서 파생된 가설로 실험을 이어간다.
   result.md 측정값이 비어 있으면 사용자에게 숫자부터 요청한다.
- 현재 기준선: `카드뉴스\20260724 여름 슈즈\result.md` (#0, 무드형 표지).
- 진행 중 실험: **#1 — 표지 무드형 vs 가격형, 판정 지표 = 저장률** (다음 게시물에 가격형 표지 적용).
- 실험 백로그 (한 번에 하나씩만):
  - **#2 — 불안 해소형 정보**: 상품 카드에 후기 기반 "사이즈 팁" 한 줄 추가 유/무 → 저장률·공유율.
    (콘텐츠 전략 근거: 소비자 구매 불안 해소 = 신뢰 = 저장. 후기 데이터에 사이즈 정보 풍부함)
  - **#3 — 검색 질문형 캡션**: 캡션 첫 줄을 "여름 쪼리 발 안 아픈 거 뭐 사지?" 같은
    실제 검색 문장으로 → 도달·프로필 방문.
  - 콘텐츠 방향성 (2026-07-24 사용자와 합의): "예쁜 것 골라주는 계정"이 아니라
    **"구매 불안을 데이터(후기·평점)로 대신 검증해주는 에디터"** 포지션. 선정 기준 공개,
    단점 한 줄 언급 허용(정직함 = 신뢰).

## 자동 생성 캡션 금지 규칙 (필수)

대시보드/로컬 앱(원클릭 파이프라인)이나 폴백 카피가 만든 **자동 캡션을 그대로 두지 않는다**
(2026-07-27 실제 발생 — 자동 캡션에 브랜드 계정·이모지 등 캡션 규칙이 하나도 반영 안 된 채
사용자에게 보여줬다가 지적받음).
- 완성본을 보여주기 전에 에이전트가 캡션을 **캡션 규칙 전체**(한 문장 흐름, 이모지 3~5개,
  브랜드 계정 목록 + 3회 검증, 해시태그 금지)에 맞게 반드시 재작성한다.
- 검증에 실패한 브랜드는 목록에서 빼고 사용자에게 알린다.

## 완성본 검토 규칙 (필수 — 사용자에게 보여주기 전 3회 검토)

완성본을 사용자에게 보여주기 **전에** 반드시 아래 3회 검토를 통과해야 한다.
검토는 렌더된 결과 이미지(1.jpg~N.jpg)를 직접 열어서 확인한다 (소스 텍스트만 보지 말 것).

1. **검토 1 — 맞춤법·띄어쓰기**: 모든 카드의 텍스트(헤드라인, 상품명, 셀링포인트, 캡션 포함)를
   전수 확인한다. 후기 인용문도 표준 띄어쓰기로 다듬는다 (예: "젤리슈즈만" → "젤리 슈즈만").
   ※ 2026-07-24 실제 발생: 후기 원문의 붙여쓰기를 그대로 실어 사용자가 지적함.
2. **검토 2 — 사실 검증**: 가격·평점·후기 수·할인율·브랜드명이 상품 데이터와 정확히 일치하는지,
   후기 인용이 원문과 일치하는지 대조한다.
3. **검토 3 — 정책·형식**: 랭킹 키워드 없음(위 키워드 규칙), 해시태그 없음, 카드 번호(N/총수) 정상,
   이미지와 상품 텍스트 매칭, 강조(<em>) 남용 없음.

4. **검토 4 — 브랜드 인스타 핸들 검증** (캡션에 계정을 넣는 경우): 각 핸들을 **서로 독립된
   출처 3곳**에서 교차 확인한다. 인정되는 출처: ① 웹 검색 결과(계정명·팔로워 확인),
   ② 브랜드 공식 홈페이지 footer 의 인스타 링크, ③ 인스타그램 프로필 페이지 직접 확인
   (계정 표시명이 브랜드명과 일치하는지), ④ 브랜드 공식 Threads/X 등 동일 핸들 사용 여부.
   세 곳 미만이면 그 브랜드 계정은 캡션에서 **빼고** 사용자에게 알린다.

하나라도 문제가 발견되면 수정 후 **3회 검토를 처음부터 다시** 통과해야 보여줄 수 있다.

## 완성본 미리보기 규칙 (필수)

카드뉴스 완성본이 나오면 반드시 아래 방식으로 사용자에게 보여준다:

1. 결과 폴더(`카드뉴스\YYYYMMDD 키워드\`) 안에 **`_preview.html`** 파일을 생성한다.
   - 모든 카드 이미지(1.jpg ~ N.jpg)와 caption.txt 내용을 한 페이지에 표시.
   - 이미지 src 에 `?t=Date.now()` 캐시 우회 쿼리를 붙여, **새로고침(F5)만 하면 항상 최신 이미지가 보이게** 한다.
2. `Start-Process "<폴더>\_preview.html"` (PowerShell) 로 열어 **기존 브라우저의 새 탭**으로 띄운다.
3. 사용자 피드백으로 카드를 수정하면:
   - 이미지(N.jpg)는 **같은 파일명으로 덮어쓴다** (파일명이 바뀌면 새로고침으로 안 보임).
   - 캡션이 바뀌면 caption.txt 와 `_preview.html` 안의 캡션 텍스트도 함께 갱신한다.
   - 창을 새로 열지 않는다 — 사용자가 이미 열린 탭을 **새로고침**해서 확인한다.
4. `_preview.html` 은 게시 대상이 아니다 (게시 스크립트는 숫자.jpg 만 올림).

## 현재 지정 키워드

| 지정일 | 키워드 |
|---|---|
| 2026-07-24 | **여름 슈즈** |

- 새 키워드가 지정되면 이 표의 맨 위에 행을 추가한다 (이력 유지).
- 사용자가 새 키워드를 말하기 전까지는 이 표의 최신 키워드를 사용한다.
