# 카드뉴스 키워드 정책 (필독)

> 이 파일은 카드뉴스를 생성하는 모든 에이전트가 반드시 따라야 하는 키워드 규칙이다.

## 규칙

1. **랭킹 키워드(랭킹픽, TOP 50, 베스트 랭킹 등)는 사용자가 명시적으로 "사용해달라"고 말하지 않는 한 절대 사용하지 않는다.**
   - 에이전트가 스스로 판단해서 랭킹 기반 주제/키워드를 고르는 것 금지.
2. **카드뉴스에는 항상 시즌성 키워드를 담는다.**
   - 계절·날씨·시기 이슈에 맞는 키워드 (예: 여름 슈즈, 우양산, 장마 아이템, 가을 자켓 등).
3. 폴더명 형식은 기존과 동일: `카드뉴스\YYYYMMDD 키워드` (예: `20260724 여름 슈즈`).

## 카드 구성 규칙 (필수)

1. **마지막 카드(CTA)의 이미지는 항상 무한도전 짤**을 사용한다.
   - 짤 위치: `fashion-cardnews\CARD\zzal\` — 이 폴더에서 **수정일 기준 최신 파일**을 쓴다.
   - 구현: `renderer.build_html` 이 `copy["cta"]["image_path"]` 를 지원한다 — CTA copy 에
     최신 짤 경로를 넣어 렌더하면 된다 (없으면 기존처럼 첫 상품 이미지 폴백).
2. **헤드라인 하이라이트(<em>)는 의미 단위로** 끊는다. 조사·어미만 홀로 강조하지 않는다.
   - 예 (2026-07-24 실제 피드백): "편한데 스타일도 <em>사는</em>" (어색) →
     "편한데<br><em>스타일도 사는</em>" (자연스러움).
3. **사용자 코멘트(note)는 참고용이다 — 그대로 싣지 않는다** (2026-07-27 사용자 피드백).
   - 코멘트의 **의도·핵심만** 가져와서, 템플릿 감성(짧고 절제된 에디토리얼 톤)으로 다듬어 쓴다.
   - 예: "더운 여름 출근길에도 단정해야해" (구어체 원문) → "더운 출근길에도<br><em>단정하게</em>".
4. **소재·스펙 등 상품 정보는 반드시 상품 상세 페이지를 확인하고 그 내용을 베이스로 쓴다**
   (2026-07-27 실제 발생 — 상세 페이지를 안 보고 상품명에서 추정해 썼다가 지적받음.
   실제 소재는 나일론 100%·메쉬 100%로 상세 페이지에 있었음).
   - 상품 데이터의 `product_url`(무신사/29CM 상세 페이지)을 **WebFetch 로 직접 열어**
     소재/혼용률/핏/디테일 정보를 확인한 뒤 작성한다. 상품명에서 추정하는 것 금지.
   - **29CM 에서 소재표가 안 잡히면** (29CM 상세는 JS 렌더라 WebFetch 로 안 잡힐 수 있음):
     **무신사에서 같은 상품을 검색해 찾고, 무신사 상세 페이지의 소재 정보를 베이스로 쓴다**
     (브랜드명 + 상품명/품번으로 검색. 나이키 등 품번(예: IF0564-248)이 있으면 품번 검색이 가장 정확).
   - 무신사에서도 같은 상품을 못 찾으면 **사용자에게 상세 페이지 내용을 물어보고**, 답을 받기
     전에는 소재 언급을 생략한 채 후기·가격 등 확실한 정보로만 쓴다.
5. **캡션 무드는 매 게시물마다 다르게 쓴다** (2026-07-27 사용자 피드백 — 직전 게시물과 같은
   문형("~부터 ~까지 꼼꼼히 따져 ~ 모았습니다")을 반복했다가 "AI 티가 난다"는 지적을 받음).
   - 캡션 작성 전 **직전 게시물 2~3개의 caption.txt 를 먼저 읽고**, 같은 문형·구조·시작 패턴을 피한다.
   - 무드를 로테이션한다 (예): 정보·큐레이션형 / 공감·일기형("결국 숏팬츠만 돌려 입고 있습니다") /
     질문 후킹형(첫 줄부터 질문) / 날씨·상황 스케치형. 같은 무드를 연속 2회 쓰지 않는다.
6. **캡션 문장은 짧게 끊어 나열하지 말고, 근거를 한 문장에 녹여 쓴다** (2026-07-24 사용자 피드백).
   - 예: "~골랐습니다 / 후기와 평점을 함께 살펴봤습니다" (뚝뚝 끊김) →
     "젤리 샌들부터 클로그까지 후기 수와 평점까지 꼼꼼히 따져 무더위에도 시원하게 신기 좋은 신발들을 모았습니다" (한 문장으로 깔끔).
7. **캡션에는 적절한 이모지를 넣는다** (2026-07-24 사용자 지시 — 기존 "0~1개" 제한보다 우선.
   2026-08-05 `pipeline/copywriter.py` 프롬프트에도 3~5개로 반영 완료).
   - 주제와 어울리는 이모지를 문장 끝에 자연스럽게 배치한다 (예: 여름 슈즈 → 🩴 ☀️, 평점 → ⭐, 댓글 유도 → 💬).
   - 브랜드 계정 목록 헤더에는 📌 를 붙인다: `📌 브랜드 계정`.
   - 과하지 않게 본문 전체 3~5개 수준. 해시태그는 여전히 금지.
8. **캡션(피드 멘트) 하단에 브랜드 인스타 계정 목록**을 넣는다.
   - 카드뉴스 **상품 순서대로**, 형식은 한 줄에 `브랜드명 @핸들` (브랜드명과 핸들 사이 띄어쓰기):
     ```
     브랜드 계정
     락피쉬웨더웨어 @rockfish_official
     ...
     ```
   - **가짜 계정 주의 — 핸들은 반드시 3회 독립 검증** 후 사용한다 (아래 검증 규칙 4번 참고).

## 디자인 세부 규칙 (2026-07-28 디자인 피드백 — 필수)

> 2026-07-28 "여름 티셔츠" 완성본 검토에서 나온 피드백. 1·2번은 템플릿
> (`card-drafts/uvparasol-insta.html`)에 반영되어 렌더러가 자동 적용한다.

1. **표지 이미지는 2번(첫 상품) 카드와 같은 사진을 그대로 재사용하지 않는다.**
   같은 원본이어도 표지는 상반신 줌 크롭으로 차별화한다 (템플릿의
   `.card.cover .photo img` 확대 크롭이 자동 처리). 첫 스와이프에서 같은 그림이
   반복되면 안 넘어간 것처럼 보이고 후킹 슬롯 하나를 낭비하게 된다.
2. **CTA(마지막) 카드는 하단 과여백 금지.** 짤 이미지를 상품 카드보다 크게
   (템플릿에서 CTA 전용 크기로 자동 적용) 써서 수직 균형을 맞춘다.
3. **회색조 누끼/플랫레이 사진이라 카드가 탁해 보이면 상세 페이지의 모델컷으로 교체한다.**
   무신사 상세 HTML의 `goodsImages` 배열(`image.msscdn.net/images/prd_img/...`)에서
   모델 착용컷을 받아 쓸 수 있다 (2026-07-28 Healing Off-Shoulder 사례).
   단, 어두운 배경에 대비가 강한 플랫레이(예: 블랙 티)는 그대로 둬도 된다.
4. **같은 가격이 3개 이상 연속되면 게시 전에 상품 데이터와 재대조한다.** 복붙 실수처럼
   보일 수 있으므로 사실 확인이 우선 (2026-07-28 스컬프터 30,160원 3종 — 실제 동일가 확인됨).
5. (선택) 헤드라인 하이라이트가 매 카드 둘째 줄에만 반복되면 기계적으로 보일 수 있다.
   카피가 자연스러운 범위에서 첫째 줄 강조도 섞는 것을 고려한다.

## 멘트·카피 세부 규칙 (2026-07-28 멘트 피드백 — 필수)

> 2026-07-28 "여름 티셔츠" 캡션을 직전 2개(7/24, 7/27)와 대조한 결과, 첫 줄 무드만
> 바뀌고 문형 뼈대가 3연속 동일했다 ("상황 스케치 → ~부터 ~까지 나열 → 질문 +
> 댓글로 알려주세요"). 무드 로테이션은 첫 줄이 아니라 캡션 전체 문형에 적용해야 한다.

1. **캡션 문형 반복 금지 (첫 줄만이 아니라 전체 구조).** 특히 아래 두 패턴은 3연속
   사용됐으므로 당분간 금지: ① 2단락의 "**A부터 B까지**" 나열 문형, ② 마무리
   "**댓글로 알려주세요**" 문구 (질문은 유지하되 표현을 매번 바꾸거나 저장 유도로 대체).
   - 단, 실험 변수 통제상 거시 구조(본문 2단락 + 질문 + 브랜드 계정 목록)는 유지한다.
     바꾸는 것은 그 안의 문형·어휘다.
2. **시그니처 단어의 연속 게시물 재사용 금지.** 직전 캡션에서 인상적으로 쓴 단어
   (예: 7/27 "결국", "돌려 입다")를 다음 게시물 표지·캡션에 또 쓰지 않는다.
   게시 전 직전 2~3개 caption.txt 를 읽을 때 문형뿐 아니라 **어휘 겹침**도 확인.
3. **표지 서브 문구와 캡션을 중복시키지 않는다.** 캡션에는 카드에 없는 새 정보
   (선정 기준, 왜 이 구성인지 — 예: 왜 단일 브랜드 5종인지)를 담는다.
4. **헤드라인은 후기 인용을 안 읽어도 이해되게 구체적으로.** 모호한 추상어보다
   구체적 사실을 쓴다 (예: "부담을 덜어낸 오프숄더" → "많이 안 파인 오프숄더").
5. **같은 게시물 안에서 카피 논리 충돌 점검.** (예: 캡션 "고민 없이 꺼냈습니다" vs
   CTA "티 고민될 땐" — 게시 전 카드+캡션을 이어 읽으며 확인한다.)
6. **캡션에서 숫자 스펙 브리핑 금지** (2026-07-28 사용자 피드백 — "너무 AI 같다").
   - "전부 ~이고, 그중 셋은 ~같아, ~만 하면 됩니다"처럼 사실을 대칭 구조로
     정리해 나열하는 문장이 대표적인 AI 티. 검증 수치(평점·후기 수·가격 비교)는
     **카드가 보여주는 정보**이므로 캡션에서 반복하지 않는다.
   - 덜 AI처럼 보이려고 구어체를 섞는 것도 금지 (확정 톤은 깔끔·절제 — 절제 톤은
     짧을수록 힘이 붙는다). 이번 건은 해당 단락을 통째로 삭제한 안을 채택했다:
     캡션은 "한 브랜드 선언 + 질문 + 계정 목록"만으로 충분했음.

## 실험·측정 규칙 (필수 — "감으로 판단" 금지)

사용자는 게시물 성과를 감으로만 판단하는 습관을 고치고 싶어 한다 (2026-07-24).
따라서 **게시물 1개 = 실험 1개**로 취급하며, 에이전트가 아래를 강제한다:

1. **게시 전**: 결과 폴더에 `result.md` 를 생성한다 (양식: [RESULT-TEMPLATE.md](RESULT-TEMPLATE.md)).
   - 가설, **바꾼 변수 1개**, 이 게시물이 속하는 **표지 풀(무드형/가격형)**, 숫자로 된 판정 기준을 게시 전에 확정해서 적는다.
   - **표지 유형은 게시물마다 번갈아 적용한다** (무드형 → 가격형 → 무드형 → …). 한쪽 유형만 연속으로 올리지 않는다.
   - **실험 중에는 변수 통제가 캡션 무드 로테이션 규칙보다 우선한다**: 실험 변수와 겹칠 수 있는
     요소(캡션 구조·게시 시간대 등)는 대조군과 최대한 동일하게 유지한다.
   - **게시 시각(분 단위)과 게시 방식(수동/자동, 자동이면 스크립트·이미지 호스팅 경로)** 을 반드시 기록한다.
2. **게시 +72시간(3일) 후**: **인사이트 API 로 직접 조회한다** (2026-07-27 권한 확인 —
   `GET graph.instagram.com/v23.0/{media_id}/insights?metric=reach,saved,shares,likes,comments,total_interactions,profile_visits,follows,views`,
   미디어 ID 는 `GET /me/media` 로 찾음. API 실패 시에만 사용자에게 숫자 요청).
   - 측정 창은 모든 게시물 **+72h 로 통일**한다 (기준선 #0이 +70h 값이므로 창을 여기에 맞춘 것. 48h 측정 금지).
   - 조회 값으로 저장률(저장÷도달)·공유율·참여율을 계산해 result.md 에 기록하고,
     **그 게시물의 저장·도달을 해당 표지 풀의 누적치에 합산**한다 (본인 저장·공유는 제외).
   - **도달 100 미만 게시물도 누적 풀에는 포함한다** — 게시물 단독 해석만 보류할 뿐, 분모·분자에서 빼지 않는다.
3. **판정은 게시물 1개가 아니라 풀끼리 누적으로 비교한다** (단발 판정 금지).
   - 각 풀의 **누적 저장률 = 누적 저장 ÷ 누적 도달**. 실험 풀 저장률이 대조 풀의 **3배 이상**이고
     실험 풀 **누적 저장이 3건 이상**이면 → 채택. 그 전까지는 계속 번갈아 게시하며 적립.
   - 게시물 1개의 결과는 "참고 신호"로만 기록한다 (그 게시물 도달 × 대조 풀 저장률 = 우연 기대치.
     실제 저장이 기대치의 3배 이상 + 2건 이상이면 우세 참고 신호).
   - **다음 게시물 설계 시**: 직전 result.md 들과 풀 누적 현황을 먼저 읽고, 거기서 파생된 가설로 이어간다.
### 🚨 2026-08-04 전수 분석에 따른 개정 — 실험 #1 보류, 지표 변경

게시물 9개 전수를 인사이트 API로 조회한 결과(`카드뉴스\_feed-analysis-20260804.md`),
아래 세 가지가 확인되어 실험 설계를 바꾼다.

1. **저장률은 현재 계정 규모에서 측정 불가능한 지표다.**
   누적 도달 1,302에 저장 1건(0.077%). 채택 조건(가격형 누적 저장 3건)을 채우려면
   가격형 풀 도달이 약 1,260 더 필요한데 현재 41이다 → 게시물 약 31개 분량.
   **→ 게시물당 도달 200+ 가 3회 연속 나오기 전까지 저장률 실험은 전면 보류한다.**
2. **실험 #1은 오염되었다.** 게시 시각을 통제하라고 규정했지만 실제로는 00:17~22:31로
   흩어졌고, 가격형 풀 2건은 둘 다 비저녁 게시다. 이대로 비교하면 시간대 효과를
   표지 효과로 오독한다. **→ #1 보류. 기존 2건은 참고 기록으로만 남긴다.**
3. **게시 시간대가 도달을 30배 가른다** (지금 통제 중이던 어떤 변수보다 크다).
   18:30~23:00 게시 = 도달 207·330·647 / 그 외 시간 = 2·6·7·16·39·48 — **구간이 겹치지 않는다.**

- **현재 대조군(2026-08-06 전수 재조회로 갱신)**: 무드형(비가격형) 풀 누적 —
  저장 1 ÷ 도달 **1,303** = **0.077%** (게시물 7개).
  (8/04 값 "1,261 / 0.079%"는 같은 게시물들의 도달이 그 뒤로도 계속 늘어난 결과 갱신됐다.
  그 이전 "1,001 / 0.10%"는 7/27 시점 값이라 폐기.)
- **가격형 풀 누적**: 저장 0 ÷ 도달 **47** (2건 — 정보량 사실상 0).
- **누적치는 게시물이 늘 때가 아니라 재조회할 때도 바뀐다.** 도달은 게시 후 몇 주에 걸쳐
  계속 오르므로(예: 7/19 롱스커트 647→661), 풀 비교를 할 때는 **같은 날 전수 재조회한 값**으로
  분모·분자를 맞춘다. 서로 다른 시점의 값을 섞어 비교하지 않는다.
- **계정 단위 배포 제한 가설은 기각됐다** (2026-08-06). 8/03 게시물의 +72h 도달이 4로 최저였지만,
  같은 시점 전수 재조회에서 나머지 9개 게시물의 도달은 모두 정상적으로 늘고 있었다
  (`20260802 여름무드\result.md` 부록 E). 저도달의 원인은 계정이 아니라 **게시 시각**이다.
- **진행 중 실험: #4 — 게시 시각 고정** (← #1 대체).
  - 가설: 저녁 19:00~21:00 KST 게시가 그 외 시간대보다 도달이 높다.
  - **판정 지표 = 저장률이 아니라 도달**(현 규모에서 유일하게 신호가 잡히는 지표).
  - 방법: 다음 3회 게시를 19:00~21:00 KST 로 고정. 표지 유형·카드 수·캡션 구조는 고정.
  - 판정: 3회 도달 중앙값이 **100 이상**이면 시간대 고정을 상수로 채택하고, 그 뒤에야
    표지·카피 A/B 실험을 재개한다.
- **게시 시각은 이제 실험 변수가 아니라 상수다.** 특별한 이유 없이 저녁 구간 밖에 게시하지 않는다.

- 실험 백로그 (한 번에 하나씩만 — **#4 판정 후에 착수**):
  - **#1 — 표지 무드형 vs 가격형** (보류. 도달 200+ 안정화 후 시간대를 고정한 채 재설계).
  - **#2 — 불안 해소형 정보**: 상품 카드에 후기 기반 "사이즈 팁" 한 줄 추가 유/무 → 저장률·공유율.
    (콘텐츠 전략 근거: 소비자 구매 불안 해소 = 신뢰 = 저장. 후기 데이터에 사이즈 정보 풍부함)
  - **#3 — 검색 질문형 캡션**: 캡션 첫 줄을 "여름 쪼리 발 안 아픈 거 뭐 사지?" 같은
    실제 검색 문장으로 → 도달·프로필 방문.
  - 콘텐츠 방향성 (2026-07-24 사용자와 합의): "예쁜 것 골라주는 계정"이 아니라
    **"구매 불안을 데이터(후기·평점)로 대신 검증해주는 에디터"** 포지션. 선정 기준 공개,
    단점 한 줄 언급 허용(정직함 = 신뢰).

## 자동 생성 캡션 금지 규칙 (필수)

대시보드/로컬 앱(원클릭 파이프라인)이나 폴백 카피가 만든 **자동 캡션을 그대로 두지 않는다**
(2026-07-27 실제 발생 — 자동 캡션에 브랜드 계정·이모지 등 캡션 규칙이 하나도 반영 안 된 채
사용자에게 보여줬다가 지적받음).
- 완성본을 보여주기 전에 에이전트가 캡션을 **캡션 규칙 전체**(한 문장 흐름, 이모지 3~5개,
  브랜드 계정 목록 + 3회 검증, 해시태그 금지)에 맞게 반드시 재작성한다.
- 검증에 실패한 브랜드는 목록에서 빼고 사용자에게 알린다.

## 완성본 검토 규칙 (필수 — 사용자에게 보여주기 전 3회 검토)

완성본을 사용자에게 보여주기 **전에** 반드시 아래 3회 검토를 통과해야 한다.
검토는 렌더된 결과 이미지(1.jpg~N.jpg)를 직접 열어서 확인한다 (소스 텍스트만 보지 말 것).

1. **검토 1 — 맞춤법·띄어쓰기**: 모든 카드의 텍스트(헤드라인, 상품명, 셀링포인트, 캡션 포함)를
   전수 확인한다. 후기 인용문도 표준 띄어쓰기로 다듬는다 (예: "젤리슈즈만" → "젤리 슈즈만").
   ※ 2026-07-24 실제 발생: 후기 원문의 붙여쓰기를 그대로 실어 사용자가 지적함.
2. **검토 2 — 사실 검증**: 가격·평점·후기 수·할인율·브랜드명이 상품 데이터와 정확히 일치하는지,
   후기 인용이 원문과 일치하는지 대조한다.
3. **검토 3 — 정책·형식**: 랭킹 키워드 없음(위 키워드 규칙), 해시태그 없음, 카드 번호(N/총수) 정상,
   이미지와 상품 텍스트 매칭, 강조(<em>) 남용 없음.

4. **검토 4 — 브랜드 인스타 핸들 검증** (캡션에 계정을 넣는 경우): 각 핸들을 **서로 독립된
   출처 3곳**에서 교차 확인한다. 인정되는 출처: ① 웹 검색 결과(계정명·팔로워 확인),
   ② 브랜드 공식 홈페이지 footer 의 인스타 링크, ③ 인스타그램 프로필 페이지 직접 확인
   (계정 표시명이 브랜드명과 일치하는지), ④ 브랜드 공식 Threads/X 등 동일 핸들 사용 여부.
   세 곳 미만이면 그 브랜드 계정은 캡션에서 **빼고** 사용자에게 알린다.

하나라도 문제가 발견되면 수정 후 **3회 검토를 처음부터 다시** 통과해야 보여줄 수 있다.

## 완성본 피드백 처리 규칙 (필수 — 2026-08-02 사용자 지시)

사용자가 "완성본 피드백"을 시키면 **문제 지적에서 멈추지 않는다. 수정까지 끝낸 뒤 보고한다.**

> 2026-08-02 실제 발생: `20260802 여름무드` 완성본에 대해 문제 12건을 정리해 보고하고
> "어느 쪽으로 갈까요?"라고 되물으며 수정은 하지 않은 채 턴을 넘겼다가 지적받음.

1. **피드백 = 검토 + 수정 + 재렌더 + 재검토까지가 한 세트다.** 지적만 나열하고
   사용자 선택을 기다리며 멈추지 않는다.
2. **선택지를 묻지 말고 판단해서 진행한다.** 수정 방향에 갈래가 있으면 에이전트가
   가장 정책에 부합하는 안을 골라 실행하고, **무엇을 왜 그렇게 고쳤는지 결과와 함께 보고**한다.
   (사용자가 다른 안을 원하면 그때 되돌리면 된다 — 물어보느라 멈추는 것이 더 나쁘다.)
3. **막혀서 진행이 불가능한 항목만** 남겨서 보고한다. 그 경우에도 나머지는 전부 고쳐놓는다.
   - 예: `ANTHROPIC_API_KEY` 부재로 Claude 카피를 못 뽑으면 → **에이전트가 직접 카피를 써서**
     재렌더한다. 키가 없다는 이유로 폴백 카피를 그대로 두지 않는다.
   - 예: 상품 원본 데이터가 없어 사실 대조가 불가능하면 → 상세 페이지를 직접 열어 확인한다.
4. **수정 후에는 3회 검토를 처음부터 다시** 돌리고, `_preview.html` 을 갱신한 뒤 보고한다.
5. 보고 형식: ① 고친 것(항목별 before → after), ② 못 고친 것과 그 이유, ③ 남은 판단 필요 사항.
   ①이 비어 있으면 그것은 피드백을 완료한 것이 아니다.

## 완성본 미리보기 규칙 (필수)

카드뉴스 완성본이 나오면 반드시 아래 방식으로 사용자에게 보여준다:

1. 결과 폴더(`카드뉴스\YYYYMMDD 키워드\`) 안에 **`_preview.html`** 파일을 생성한다.
   - 모든 카드 이미지(1.jpg ~ N.jpg)와 caption.txt 내용을 한 페이지에 표시.
   - 이미지 src 에 `?t=Date.now()` 캐시 우회 쿼리를 붙여, **새로고침(F5)만 하면 항상 최신 이미지가 보이게** 한다.
2. `Start-Process "<폴더>\_preview.html"` (PowerShell) 로 열어 **기존 브라우저의 새 탭**으로 띄운다.
3. 사용자 피드백으로 카드를 수정하면:
   - 이미지(N.jpg)는 **같은 파일명으로 덮어쓴다** (파일명이 바뀌면 새로고침으로 안 보임).
   - 캡션이 바뀌면 caption.txt 와 `_preview.html` 안의 캡션 텍스트도 함께 갱신한다.
   - 창을 새로 열지 않는다 — 사용자가 이미 열린 탭을 **새로고침**해서 확인한다.
4. `_preview.html` 은 게시 대상이 아니다 (게시 스크립트는 숫자.jpg 만 올림).

## 현재 지정 키워드

| 지정일 | 키워드 |
|---|---|
| 2026-08-06 | **한여름 시스루 셔츠** (렌더 완료·게시 대기 — `20260806 한여름 시스루 셔츠`) |
| 2026-08-05 | **한여름 냉감 이너** (게시 완료 — `20260805 한여름 냉감 이너`) |
| 2026-08-02 | **여름 롱 원피스·스커트** (게시 완료 — `20260802 여름무드`) |
| 2026-07-28 | **여름 티셔츠** (사용자 지정 상품 — 스컬프터 5종) |
| 2026-07-24 | **여름 슈즈** |

- 새 키워드가 지정되면 이 표의 맨 위에 행을 추가한다 (이력 유지).
- 사용자가 새 키워드를 말하기 전까지는 이 표의 최신 키워드를 사용한다.
