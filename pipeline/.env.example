# 선택: 있으면 Claude 로 문구 생성, 없으면 규칙 기반 폴백
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# 선택: 렌더가 끝나면 미리보기 창을 자동으로 띄운다 (기본 꺼짐 — 탭이 쌓이는 것을 막기 위함).
# 꺼져 있어도 미리보기 주소는 앱 콘솔 로그와 POST /api/selections 응답에 찍힌다.
# PIPELINE_AUTO_OPEN=1
