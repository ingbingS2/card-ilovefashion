# 실험 로그 템플릿 (result.md 양식)

> 카드뉴스 게시물 = 실험 1개. 게시할 때마다 결과 폴더에 이 양식으로 `result.md` 를 만든다.
> "실험 설계"는 **게시 전에** 에이전트가 채우고, 측정값은 사용자가 인스타 앱에서 불러주면
> 에이전트가 계산·판정까지 채운다. **감으로 판정 금지 — 판정 기준은 게시 전에 숫자로 박는다.**

---

# 실험 로그 — {YYYYMMDD 키워드}

## 1. 실험 설계 (게시 전 작성)
- **실험 번호**: #N  ← 현재 진행 중인 실험은 **#4 (게시 시각 19:00~21:00 KST 고정, 판정 지표 = 도달)**
- **가설**: (예: 저녁 19:00~21:00 게시가 그 외 시간대보다 도달이 높다)
- **바꾼 변수 (반드시 1개만)**: (예: 게시 시각 → 저녁 구간 고정)
- **대조군**: #M {폴더명} (그 외 조건은 동일하게 유지: 표지 유형, 카드 장수, 캡션 구조)
- **판정 기준 (게시 전에 숫자로 확정)**: (예: 3회 도달 중앙값이 100 이상이면 "채택")
  - ⚠️ **저장률은 판정 지표로 쓰지 않는다** — 현 계정 규모(2026-08-04 전수 분석 시점 누적 도달
    1,302·저장 1)에서는 측정 자체가 불가능해 보류됐다. 도달 200+ 가 3회 연속 나온 뒤 재개.

## 2. 게시 정보
- 게시 일시: YYYY-MM-DD HH:MM (**분 단위까지** — 시간대가 현재 최대 변수다)
- 게시 방식: 자동(post_ig.py) / 수동 — 자동이면 이미지 호스팅 경로(litterbox/uguu)도 기록

## 3. 측정값 — 게시 +72시간 후
> **인사이트 API 로 에이전트가 직접 조회한다** (`/me/media` 로 media_id → `/{id}/insights`).
> 측정 창은 모든 게시물 **+72h 로 통일**한다 (48h 측정 금지 — KEYWORD-POLICY §실험·측정 규칙 2).
> API 조회가 실패할 때만 사용자에게 인스타 앱 인사이트 숫자를 요청한다.

| 지표 | +72시간 | 7일 (선택) |
|---|---|---|
| 도달 (Reach) | | |
| 저장 | | |
| 공유 | | |
| 좋아요 | | |
| 댓글 | | |
| 프로필 방문 | | |
| 팔로우 | | |

## 4. 계산 지표 (Claude 가 계산)
- **도달** → ___ (실험 #4 판정 지표. 저녁 게시 3회 중앙값 100 이상이면 시간대 고정 채택)
- **저장률** = 저장 ÷ 도달 → ___% (대조군 무드형 풀 누적: **0.077%** = 저장 1 ÷ 도달 1,303, 2026-08-06 전수 재조회)
  - ⚠️ 누적치는 게시물이 늘 때만이 아니라 **재조회할 때도 바뀐다**(도달은 게시 후 몇 주간 계속 오른다).
    이 숫자는 스냅샷이므로, 실제 비교 시에는 KEYWORD-POLICY.md 의 최신 값을 기준으로 삼는다.
- 공유율 = 공유 ÷ 도달 → ___%
- 참여율 = (좋아요+댓글+저장+공유) ÷ 도달 → ___%
- 팔로우 전환율 = 팔로우 ÷ 프로필 방문 → ___%

## 5. 판정과 배움 (Claude 가 초안 작성, 사용자 확인)
- **판정**: 채택 / 기각 / 판단불가(표본 부족·변수 오염) — 근거 숫자 명시
- **배움 한 줄**: (면접에서 그대로 말할 수 있는 문장으로)
- **다음 실험**: (이번 결과에서 파생되는 다음 가설)
